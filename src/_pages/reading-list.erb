---<%
{
  layout: :page,
  title: "2022 Reading List",
  description: "Documenting some of the articles I read online to share with others.",
  permalink: "/reading-list/",
  no_index: false,
}
%>---

<%= render List.new do %>
  <% site.data.raindrop.reading_list.items.each do |item| %>
    <%= render ListTile.new(url: item.link, title: item.title, image: item&.cover, description: nil, clamp: false) %>
  <% end %>
<% end %>
