<%= render Entry.new(
  density: :media,
  href: listen_url,
  external: true,
  mark: {
    label: field(:mark),
    hue: hue,
    image: field(:image),
    dim: !field(:active),
  },
  title: field(:name),
  excerpt: field(:tagline),
  meta: facts,
  trailing: field(:role),
) %>
