---
name: post
description: a post
variables:
  post: object
---

<div class="flex flex-col space-y-2">
  <a class="text-blue-600 dark:text-blue-500" href="{{ post.url }}">{{ post.title }}</a>
  <p class="clamp-2">{{ post.excerpt }}</p>
  <p class="p-small">{{ post.date }}</p>
</div>
