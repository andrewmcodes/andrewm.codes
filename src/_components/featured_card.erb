<%= render Tile.new(
  title: field(:title),
  href: field(:href),
  desc: field(:desc),
  chip: field(:kind),
) do %>
  <% if field(:label) %>
    <span><%= field(:label) %></span>
  <% end %>
<% end %>
