<figure class="not-prose space-y-1">
  <div class="<%= aspect %>">
    <%= slotted :image %>
  </div>

  <% if slotted?(:caption) %>
    <figcaption class="text-center text-note text-mauve-11">
      <%= slotted :caption %>
    </figcaption>
  <% end %>
</figure>
