<%# One destination, so the whole row is the link and carries no inner action. %>
<%= render Entry.new(
  density: :full,
  href: url,
  external: true,
  lead: date_str,
  lead_sub: duration_str,
  kicker: kicker,
  title: title,
  excerpt: excerpt,
) %>
