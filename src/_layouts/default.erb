<!DOCTYPE html>
<html lang="en" class="h-full" data-env="<%= Bridgetown.env %>">
  <%= render "head" %>
  <body class="h-full font-sans bg-skin text-skin <%= class_map "debug-screens": Bridgetown.env.development? %>">
    <%= render Alert.new(level: site.metadata.alert.level).with_content(site.metadata.alert.message) if site.metadata.alert.level %>
    <div class="flex flex-col min-h-full">
      <%= render "nav" %>
      <main class="flex flex-col flex-grow px-4 mt-12">
        <%= yield %>
      </main>
      <%= render "footer" %>
    </div>
  </body>
</html>
