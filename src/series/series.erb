---
layout: page
title: "Series: :prototype-term"
prototype:
  term: series
  collection: posts
---

<%= render List.new do %>
  <%= render(PostComponent.with_collection(paginator.resources)) %>
<% end %>
